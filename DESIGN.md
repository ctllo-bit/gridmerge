# 2048 游戏设计结构

## 目标

参考原版 2048 的核心体验，制作一个响应式网页小游戏，支持桌面、平板、手机使用，并具备流畅动画与音效反馈。

## 页面结构

- `index.html`
  - 页面入口
  - 游戏标题、分数区域、控制按钮
  - 4x4 游戏棋盘
  - 游戏状态遮罩，包括胜利、失败、继续游戏
- `styles.css`
  - 响应式布局
  - 棋盘、方块、按钮、遮罩视觉样式
  - 移动、合并、新生方块动画
- `script.js`
  - 游戏状态管理
  - 方块移动、合并、生成逻辑
  - 键盘与触摸滑动输入
  - 动画调度
  - 音效生成与播放

## 核心功能

1. 棋盘
   - 固定 4x4 网格。
   - 方块严格对齐网格。
   - 使用 CSS 变量控制格子尺寸和间距，保证不同屏幕下比例一致。

2. 输入
   - 桌面端支持方向键和 WASD。
   - 移动端支持上下左右滑动。
   - 滑动距离达到阈值后触发移动，避免误触。

3. 游戏规则
   - 每次有效移动后随机生成一个新方块。
   - 新方块为 2 或 4，2 的概率更高。
   - 相同数字在一次移动中只合并一次。
   - 达到 2048 触发胜利提示。
   - 无可移动空间且无法合并时触发失败提示。

4. 动画
   - 方块移动使用 transform 平滑过渡。
   - 合并方块使用弹性放大动画。
   - 新生方块使用缩放出现动画。
   - 棋盘不会因动画产生布局抖动。

5. 音效
   - 有效滑动播放轻快移动音。
   - 合并播放更明亮的合成音。
   - 胜利播放上扬音阶。
   - 失败播放低沉收束音。
   - 使用 Web Audio API 生成音效，不依赖外部音频文件。

## 响应式方案

- 棋盘宽度使用 `min(92vw, 500px)`。
- 小屏幕上压缩标题和按钮间距。
- 方块数字根据屏幕尺寸自动缩放。
- 触摸操作禁用页面滚动干扰。

## 状态模型

- `grid`: 4x4 二维数组，存储方块对象或空值。
- `tiles`: 方块对象集合，包含 id、数字、坐标、动画状态。
- `score`: 当前分数。
- `bestScore`: 浏览器本地最高分。
- `won`: 是否已达成 2048。
- `gameOver`: 是否失败。

## 视觉风格

- 保留原版 2048 的温暖棋盘与数字方块风格。
- 增强按钮、分数牌和遮罩的现代感。
- 动画节奏轻快但不过度夸张，重点服务操作反馈。
